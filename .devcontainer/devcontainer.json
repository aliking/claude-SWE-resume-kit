{
  "$schema": "https://raw.githubusercontent.com/devcontainers/spec/main/schemas/devContainer.schema.json",
  "name": "claude-SWE-resume-kit",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteUser": "vscode",
  "runArgs": [
    "--init"
  ],
  "mounts": [
    "source=${localWorkspaceFolder}/.devcontainer/claude-settings.json,target=/home/vscode/.claude/settings.json,type=bind,consistency=cached"
  ],
  "containerEnv": {
    "CLAUDE_CONFIG_DIR": "/home/vscode/.claude"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "james-yu.latex-workshop",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "chat.tools.terminal.autoApprove": {
          "^ls( .*)?$": true,
          "^pwd$": true,
          "^find( .*)?$": true,
          "^rg( .*)?$": true,
          "^cat( .*)?$": true,
          "^head( .*)?$": true,
          "^tail( .*)?$": true,
          "^sed -n( .*)?$": true,
          "^wc( .*)?$": true,
          "^command -v( .*)?$": true,
          "^git (status|diff|log|show)( .*)?$": true,
          "^node --version$": true,
          "^npm --version$": true,
          "^pdflatex --version$": true,
          "/^bash scripts\\/safe-run\\.sh/": { "approve": true, "matchCommandLine": true },
          "^npm install( .*)?$": false,
          "^apt-get( .*)?$": false,
          "^rm( .*)?$": false,
          "^mv( .*)?$": false,
          "^cp( .*)?$": false,
          "^git add( .*)?$": false,
          "^curl( .*)?$": false,
          "^wget( .*)?$": false,
          "^git (commit|merge|push|reset|rebase)( .*)?$": false
        },
        "chat.tools.edits.autoApprove": {
          ".devcontainer/devcontainer.json": false,
          ".devcontainer/Dockerfile": false,
          ".devcontainer/claude-settings.json": false,
          "scripts/safe-run.sh": false,
          "scripts/generate_claude_settings.js": false
        }
      }
    }
  },
  "postCreateCommand": "node /workspaces/claude-SWE-resume-kit/scripts/generate_claude_settings.js && pdflatex --version && node --version && python3 --version"
}
